<?xml version="1.0" encoding="utf-8"?>
<!--
  :service-android module manifest.

  This module declares the foreground receiver service (#21), the
  associated permissions, and the legacy storage permission used by
  LegacyDownloadsEnvironment on API 24-28 (#23). Notification channel
  registration happens at runtime; channels are not declared in the
  manifest on any current Android version.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Shizuku (Path B — the in-app privileged radio path that replaces the
      radio-helper APK when Shizuku is present; see docs/SHIZUKU_PREFERRED_PATH_PLAN.md).
      overrideLibrary keeps the Shizuku AARs' higher minSdk from blocking this
      module's minSdk 24. API_V23 is the permission required to talk to the
      Shizuku/Sui service. (Verbatim from the radio-helper manifest.)
    -->
    <uses-sdk tools:overrideLibrary="rikka.shizuku.provider, rikka.shizuku.api, rikka.shizuku.shared, rikka.shizuku.aidl" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <!--
      Legacy-storage write permission used by LegacyDownloadsEnvironment
      on API 24-28 (where MediaStore.Downloads does not yet exist).

      maxSdkVersion="28" is critical: the permission is a no-op on API
      29+ and requesting it there would only confuse the user with an
      irrelevant runtime prompt. MediaStoreDownloadsEnvironment uses
      MediaStore.Downloads inserts which require no storage permission
      on Q and above.
    -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!--
      Foreground service permission (#21). Required to call
      Service.startForeground() on API 28+. No runtime prompt — declared
      in the manifest is sufficient.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!--
      Foreground service connectedDevice subtype permission (#21).
      Required on API 34+ when the service declares
      foregroundServiceType="connectedDevice". Older platforms do not
      know about this permission, so we gate the manifest entry with
      tools:targetApi to avoid lint warnings on lower minSdk builds.
    -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"
        tools:targetApi="34" />

    <application>

        <!--
          Persistent foreground service that hosts the inbound Quick
          Share listener. Type connectedDevice covers peer-to-peer file
          transfer over Wi-Fi (and, in Phase 2, BLE). exported=false
          because the service is internal — the only entry points are
          the convenience helpers on ReceiverForegroundService.Companion
          inside the same package.
        -->
        <service
            android:name=".receiver.ReceiverForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Shizuku provider (required by the Shizuku API to talk to the
             Shizuku/Sui service). The RadioShellService user service is NOT
             declared here — Shizuku spawns it via app_process. Authority is
             per-app (applicationId + ".shizuku") so it can't collide with the
             separate radio-helper APK's provider. (Verbatim from radio-helper.) -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

    </application>

</manifest>
