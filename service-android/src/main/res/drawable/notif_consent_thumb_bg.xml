<?xml version="1.0" encoding="utf-8"?>
<!-- Placeholder thumbnail square for the BRIDGE-style consent heads-up
     (option 2), mirroring the ReceiveCard's plain gray thumb.
     A real file thumbnail isn't available before the transfer is accepted, so
     this is a static rounded placeholder. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FF45454D" />
    <corners android:radius="12dp" />
</shape>
